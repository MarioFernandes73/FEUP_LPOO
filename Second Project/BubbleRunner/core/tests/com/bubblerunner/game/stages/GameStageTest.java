package com.bubblerunner.game.stages;

import com.badlogic.gdx.Game;
import com.bubblerunner.game.BubbleRunner;

import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by Mario on 03/06/2017.
 */
public class GameStageTest {

    @Test
    public void testGraphicsManagerSingleton(){

        //GameStage stage = new GameStage(new BubbleRunner());
       // assertNotNull(stage);
       // assertNotNull(stage.getGraphicsManager());
       // assertNotNull(stage.getGraphicsManager().gameGraphics);
       // assertNotNull(stage.getGraphicsManager().hud);

    }

}